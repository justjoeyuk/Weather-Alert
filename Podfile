source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '8.1'

install! 'cocoapods',
         :deterministic_uuids => false,
         :integrate_targets => false

target 'Weather Alert' do
  pod 'GoogleMaps'
end
